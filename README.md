# 🌊 HydroWeb v2.0: Ultimate Destruction Complex

HydroWeb v2.0 — это высокопроизводительный, оптимизированный фреймворк для аудита безопасности веб-ресурсов, созданный для **максимального воздействия** на цель при **минимальной нагрузке** на вашу локальную систему.

В этой версии **отключены** ресурсоемкие методы (например, полный логин-флуд) для обеспечения стабильности, а упор сделан на **критические векторы эксплуатации**, способные вывести цель из строя.

---

## ⚡ Особенности и Преимущества

* **Оптимизация CPU:** Полностью **отключен** ресурсоемкий модуль `Auth DoS` (логин-флуд). Это гарантирует, что HydroWeb не будет нагружать ваш процессор на 100%.
* **Динамический Slow DoS:** Используется **адаптивное** управление потоками (`THREAD_COUNT=100`) и увеличенные таймауты (`20s`) для **Slow HTTP DoS**. Это снижает локальную нагрузку, сохраняя при этом эффективность атаки на истощение ресурсов цели.
* **Decimation Engine:** Встроенный модуль эксплуатации, который автоматически использует **тяжелые, деструктивные уязвимости** при их обнаружении.

---

## 💣 Ключевой Функционал (Decimation Vectors)

HydroWeb не просто сканирует; он ищет векторы, способные **обрушить** веб-сервер с одного узла (при наличии уязвимости):

| Вектор Эксплуатации | Описание / Воздействие |
| :--- | :--- |
| **RCE/SSTI DoS** | Введение **Fork Bomb** или команд ввода/вывода для критической нагрузки на CPU сервера. |
| **XML Bomb ($\text{XXE}$)** | Отправка **рекурсивного $\text{XML}$-пейлоада** для истощения оперативной памяти сервера (OOM). |
| **NoSQLi DoS** | Внедрение **вечных циклов** (`$where: 'while (true) {}'`) в запросы к базе данных $\text{NoSQL}$ для потребления $\text{CPU}$ базы данных. |
| **Persistent Slow DoS** | Поддержание большого числа полуоткрытых $\text{HTTP}$-соединений для истощения пула потоков веб-сервера. |
| **Time-Based SQLi** | Аудит $\text{SQL}$ с использованием задержек для **беззвучного** подтверждения уязвимости. |

---

## 🚀 Установка и Запуск

### Требования

Для работы необходимы библиотеки `requests` и `rich`.

```bash
pip install requests rich
